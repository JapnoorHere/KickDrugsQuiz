<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Registration</title>
    <style>
        * {
            font-family: Arial, Helvetica, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-image: url(logo.png);
            background-blend-mode: soft-light;
            background-repeat: no-repeat;
            background-position: center;
            background-color: rgba(255, 242, 0, 0.5);
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 20px;
            width: 100%;
            max-width: 500px;
        }

        .card {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        h1 {
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
        }

        form {
            display: flex;
            flex-direction: column;
            width: 100%;
            gap: 1rem;
        }

        input {
            padding: 1rem;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(109, 150, 254, 0.5);
            font-size: 1rem;
        }

        button {
            background-color: #6d96fe;
            padding: 1rem;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            transition: all 0.2s ease-in-out;
        }

        button:hover {
            cursor: pointer;
            background-color: rgb(0, 72, 229);
        }

        @media (max-width: 768px) {
            .card {
                padding: 1.5rem;
            }

            h1 {
                font-size: 1.25rem;
            }

            input, button {
                padding: 0.75rem;
                font-size: 0.875rem;
            }
        }

        @media (max-width: 480px) {
            .card {
                padding: 1rem;
            }

            h1 {
                font-size: 1rem;
            }

            input, button {
                padding: 0.5rem;
                font-size: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <h1>Enter details to start Quiz</h1>
            <form action="/details" method="post">
                <input type="text" placeholder="Name" name="name" required>
                <input type="email" placeholder="Email" name="email" required>
                <input type="tel" placeholder="Phone Number" name="phone" required>
                <input type="text" placeholder="School/University" name="institution" required>
                <button type="submit">START</button>
            </form>
        </div>
    </div>
</body>
</html>
